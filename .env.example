# Optional: used by Devvit CLI during local playtest
# DEVVIT_SUBREDDIT=your_test_subreddit

# OpenAI credentials are configured via Devvit global secrets (not this file):
#   npx devvit settings set openaiApiKey
#   npx devvit settings set openaiModel   # optional; default gpt-4o-mini
